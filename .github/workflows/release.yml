# GENERATED BY: cargo xtask ci
# DO NOT EDIT - edit xtask/src/ci.rs instead

---
name: Release
"on": 
  push: 
    tags: 
      - v*

  pull_request: 
    branches: 
      - main

  workflow_dispatch: 
permissions: 
  contents: write

env: 
  CARGO_TERM_COLOR: always

jobs: 
  build-linux-x64: 
    name: Build (linux-x64)
    runs-on: depot-ubuntu-24.04-32
    steps: 
      - 
        name: Checkout
        uses: actions/checkout@v4
      - 
        name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with: 
          targets: x86_64-unknown-linux-gnu

      - 
        name: Rust cache
        uses: Swatinem/rust-cache@v2
      - 
        name: Build WASM crates
        run: bash scripts/build-wasm.sh
        shell: bash
      - 
        name: Build ddc and plugins
        run: bash scripts/build-target.sh x86_64-unknown-linux-gnu
        shell: bash
      - 
        name: Assemble archive
        run: bash scripts/assemble-archive.sh x86_64-unknown-linux-gnu
        shell: bash
      - 
        name: Upload artifact
        uses: actions/upload-artifact@v4
        with: 
          name: build-linux-x64
          path: dodeca-x86_64-unknown-linux-gnu.tar.xz


  build-linux-arm64: 
    name: Build (linux-arm64)
    runs-on: depot-ubuntu-24.04-arm-32
    steps: 
      - 
        name: Checkout
        uses: actions/checkout@v4
      - 
        name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with: 
          targets: aarch64-unknown-linux-gnu

      - 
        name: Install cross-compilation tools
        run: sudo apt-get update && sudo apt-get install -y gcc-aarch64-linux-gnu
      - 
        name: Rust cache
        uses: Swatinem/rust-cache@v2
      - 
        name: Build WASM crates
        run: bash scripts/build-wasm.sh
        shell: bash
      - 
        name: Build ddc and plugins
        run: bash scripts/build-target.sh aarch64-unknown-linux-gnu
        shell: bash
      - 
        name: Assemble archive
        run: bash scripts/assemble-archive.sh aarch64-unknown-linux-gnu
        shell: bash
      - 
        name: Upload artifact
        uses: actions/upload-artifact@v4
        with: 
          name: build-linux-arm64
          path: dodeca-aarch64-unknown-linux-gnu.tar.xz


  build-macos-arm64: 
    name: Build (macos-arm64)
    runs-on: depot-macos-latest
    steps: 
      - 
        name: Checkout
        uses: actions/checkout@v4
      - 
        name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with: 
          targets: aarch64-apple-darwin

      - 
        name: Rust cache
        uses: Swatinem/rust-cache@v2
      - 
        name: Build WASM crates
        run: bash scripts/build-wasm.sh
        shell: bash
      - 
        name: Build ddc and plugins
        run: bash scripts/build-target.sh aarch64-apple-darwin
        shell: bash
      - 
        name: Assemble archive
        run: bash scripts/assemble-archive.sh aarch64-apple-darwin
        shell: bash
      - 
        name: Upload artifact
        uses: actions/upload-artifact@v4
        with: 
          name: build-macos-arm64
          path: dodeca-aarch64-apple-darwin.tar.xz


  build-windows-x64: 
    name: Build (windows-x64)
    runs-on: depot-windows-2022-16
    env: 
      RUSTFLAGS: "-Ctarget-feature=+crt-static"

    steps: 
      - 
        name: Checkout
        uses: actions/checkout@v4
      - 
        name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with: 
          targets: x86_64-pc-windows-msvc

      - 
        name: Rust cache
        uses: Swatinem/rust-cache@v2
      - 
        name: Build WASM crates
        run: bash scripts/build-wasm.sh
        shell: bash
      - 
        name: Build ddc and plugins
        run: bash scripts/build-target.sh x86_64-pc-windows-msvc
        shell: bash
      - 
        name: Assemble archive
        run: bash scripts/assemble-archive.sh x86_64-pc-windows-msvc
        shell: bash
      - 
        name: Upload artifact
        uses: actions/upload-artifact@v4
        with: 
          name: build-windows-x64
          path: dodeca-x86_64-pc-windows-msvc.zip


  release: 
    name: Create Release
    runs-on: ubuntu-latest
    needs: 
      - build-linux-x64
      - build-linux-arm64
      - build-macos-arm64
      - build-windows-x64

    if: "startsWith(github.ref, 'refs/tags/')"
    env: 
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}

    steps: 
      - 
        name: Checkout
        uses: actions/checkout@v4
      - 
        name: Download all artifacts
        uses: actions/download-artifact@v4
        with: 
          path: dist
          pattern: build-*
          merge-multiple: "true"

      - 
        name: Prepare release
        run: bash scripts/release.sh
        shell: bash
      - 
        name: Create GitHub Release
        run: "gh release create \"${{ github.ref_name }}\" \\\n  --title \"dodeca ${{ github.ref_name }}\" \\\n  --generate-notes \\\n  dist/*"
        shell: bash
      - 
        name: Update Homebrew tap
        run: "bash scripts/update-homebrew.sh \"${{ github.ref_name }}\""
        shell: bash

